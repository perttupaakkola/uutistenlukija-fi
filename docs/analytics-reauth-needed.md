# Analytics/Search Console reauth needed

Generated by Hermes on 2026-04-30.

## Current state

The reporting jobs can now find the token files and project environment, but both Google OAuth refresh tokens are rejected by Google:

- `.secrets/analytics-tokens.json`: `invalid_grant`
- `.secrets/search-console-tokens.json`: `invalid_grant`

This usually means the Google account revoked access, the OAuth app was reset, or the refresh token expired/was superseded.

## Impact

- GA4 daily traffic cards cannot fetch real data.
- Search Console fetch cannot refresh, so CTR reports fall back to synthetic/frontmatter analysis.
- Business feedback loop remains partly blind until reauth is done.

## Safe next action

Re-authorize Google Analytics and Search Console OAuth for the `uutistenlukija.fi` property and replace these files without changing their schema:

- `/home/pertt/.openclaw/workspace/.secrets/analytics-tokens.json`
- `/home/pertt/.openclaw/workspace/.secrets/search-console-tokens.json`

Required fields:

```json
{
  "client_id": "...",
  "client_secret": "...",
  "refresh_token": "...",
  "access_token": "...",
  "scope": "analytics.readonly or webmasters.readonly"
}
```

After reauth, verify with:

```bash
cd /home/pertt/.openclaw/workspace/projects/uutistenlukija
scripts/run_with_project_env.sh python3 scripts/daily_traffic_card.py --dry-run
scripts/run_with_project_env.sh python3 pipeline/fetch_search_console.py
scripts/run_with_project_env.sh python3 scripts/ctr_gap_report.py --post-discord
```

Do not print token values in logs or chat.
